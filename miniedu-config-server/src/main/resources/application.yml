spring:
  application:
    name: config-server
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:git}
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_REPO_URI}
          clone-on-start: true
        native:
          search-locations: file:/config-repo

server:
  port: 8888